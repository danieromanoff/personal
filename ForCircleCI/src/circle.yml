machine:
  node:
    version: 4.1.0
  java:
    version: oraclejdk8
dependencies:
  override:
    - npm install
    - bower install
    - npm install -g gulp
    - gulp build
test:
  override:
    - ant -lib lib/ -buildfile build/build.xml Build: 
        pwd: ../../
        timeout: 600
